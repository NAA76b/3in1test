# Requirements for MASTER 3in1 Tool v2
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=1.3.0
openpyxl>=3.0.9

# Fuzzy matching for employee ID lookup
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.2  # Optional but recommended for faster fuzzy matching

# Windows COM integration (for Outlook)
pywin32>=301; platform_system == "Windows"

# Document generation
python-docx>=0.8.11

# GUI requirements (tkinter comes with Python)